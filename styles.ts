import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
      flex: 1,
      backgroundColor: '#fff',
      justifyContent: 'center',
      padding: 32,
      gap: 16,
    },
    input: {
        borderWidth: 1,
        borderColor: '#333',
        borderRadius: 5,
        height: 56,
        padding: 16,
        fontSize: 18,
    },
    text: {
        textAlign: 'center',
        fontWeight: 'bold',
        fontSize: 18,
    }
  });